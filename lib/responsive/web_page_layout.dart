import 'package:flutter/material.dart';

class WebPageLayout extends StatelessWidget {
  const WebPageLayout({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('this is web'),
      ),
    );
  }
}
